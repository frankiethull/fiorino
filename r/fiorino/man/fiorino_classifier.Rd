% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fiorino.R
\name{fiorino_classifier}
\alias{fiorino_classifier}
\alias{fiorino_regressor}
\title{Fiorino tabular foundation models (reticulate bridge)}
\usage{
fiorino_classifier(
  checkpoint = NULL,
  repo_id = "Nanite-Labs/nanites-fiorino-tabular",
  device = "auto",
  seed = 0,
  max_rows = 2048
)

fiorino_regressor(
  checkpoint = NULL,
  repo_id = "Nanite-Labs/nanites-fiorino-tabular",
  device = "auto",
  seed = 0,
  max_rows = 2048
)
}
\arguments{
\item{checkpoint}{Local weights file (\code{.pt} / \code{.safetensors}), an HF repo
id, or \code{NULL} (default) to resolve the bifronte head automatically.}

\item{repo_id}{Hugging Face repo used when \code{checkpoint} is \code{NULL}.}

\item{device}{\code{"auto"} (default), \code{"cpu"}, or \code{"cuda"}.}

\item{seed}{Random seed for context subsampling.}

\item{max_rows}{Max context rows (subsampled, default 2048).}
}
\value{
An object of class \code{fiorino_classifier} / \code{fiorino_regressor}
wrapping the Python estimator.
}
\description{
\code{fiorino_classifier()} and \code{fiorino_regressor()} build in-context tabular
learners backed by the Python \code{fiorino} package (FiorinoNano). \code{fio_fit()}
stores the training set as the in-context prompt (no gradients);
\code{fio_predict()} / \code{fio_predict_proba()} answer queries in one forward pass.

Weights download once from Hugging Face
(\code{Nanite-Labs/nanites-fiorino-tabular}) on first fit.
}
\examples{
\dontrun{
clf <- fiorino_classifier()
clf <- fio_fit(clf, iris[, 1:4], iris$Species)
fio_predict(clf, iris[1:5, 1:4])
}
}
