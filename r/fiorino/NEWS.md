# fiorino 0.1.0

* Initial release: reticulate bridge to the Python `fiorino` package.
* `fiorino_classifier()` / `fiorino_regressor()` with `fio_fit()`,
  `fio_predict()`, `fio_predict_proba()`.
* Weights resolve from Hugging Face
  (`Nanite-Labs/nanites-fiorino-tabular`, bifronte heads) on first fit.
